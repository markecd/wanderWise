version: '3'
services:
  backend:
    build: ./backend
    ports:
      - "6500:6500"
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    volumes:
      - ./backend/dbConn:/usr/src/app/dbConn
      - ./backend/wanderwisedb-firebase-adminsdk-gz1zt-f9c1384928.json:/usr/src/app/wanderwisedb-firebase-adminsdk-gz1zt-f9c1384928.json

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    env_file:
      - .env
    environment:
      - NODE_ENV=development
